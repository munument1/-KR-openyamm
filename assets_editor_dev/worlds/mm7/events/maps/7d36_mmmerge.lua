-- MMMerge map supplement: corrected Land of the Giants exit coordinates.

ReplaceMapEvent(501, "Leave the Cave", function()
    evt.MoveToMap(20890, -3119, 5, 896, 0, 0, 0, 3, "out12.odm")
end, "Leave the Cave")
